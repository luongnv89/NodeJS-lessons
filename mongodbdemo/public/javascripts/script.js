function editAction (rsId) {
	console.log("Edit radio id: " + rsId);
}

function deleteAction (rsId) {
	console.log("Delete radio id: " + rsId);
}

function playAction (rsId) {
	console.log("Play radio id: " + rsId);
}